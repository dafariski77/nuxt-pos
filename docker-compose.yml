version: '3.8'

services:
  gravitypos:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gravitypos-app
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - .env
